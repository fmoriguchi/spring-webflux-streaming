version: "3.3"

services:
  streamer:
      image: fmoriguchi/streamer-videos:0.0.2
      build: .
      ports:
        - "8080:8080"
      volumes:
        - file-repository:/tmp

volumes:
  file-repository: